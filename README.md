# Product Banner App

A modern Android application built with **Kotlin** and **Jetpack Compose**. This app displays a promotional product banner featuring a full-screen background image, a readability gradient, and stylized text overlays.

## 📱 Project Overview
This project demonstrates UI layout techniques in Jetpack Compose, specifically:
* Layering components using `Box`.
* Aligning text and gradients over images.
* Styling typography and containers.

## 📸 Screenshot
![App Screenshot](screenshot.png)
*(Make sure to upload your screenshot.png to the repository for this image to show up)*

## 🛠️ Tech Stack
* **Language:** Kotlin
* **UI Toolkit:** Jetpack Compose
* **IDE:** Android Studio Ladybug (or newer)

## 🚀 How to Run
1.  **Clone the repository**:
    ```bash
    git clone [https://github.com/YOUR_USERNAME/ProductBannerApp.git](https://github.com/YOUR_USERNAME/ProductBannerApp.git)
    ```
2.  **Open in Android Studio**:
    * Select `Open` and navigate to the project folder.
3.  **Add the Asset**:
    * Ensure you have an image file named `headphone_bg.jpg` in the folder: `app/src/main/res/drawable/`.
4.  **Run the App**:
    * Connect an Android device or start an Emulator.
    * Click the green **Run** (Play) button.

## 📂 Project Structure
* **`MainActivity.kt`**: Contains all the UI logic, including the `ProductBannerScreen` composable function.
* **`res/drawable`**: Stores the background image asset.

## 👤 Author
[Your Name]
